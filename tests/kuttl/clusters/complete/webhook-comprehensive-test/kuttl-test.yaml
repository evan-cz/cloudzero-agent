apiVersion: kuttl.dev/v1beta1
kind: TestSuite
metadata:
  name: webhook-comprehensive-test
  annotations:
    kuttl.dev/type: "test"
spec:
  testDirs:
    - ./steps
  timeout: 600
  parallel: 1
  reportFormat: JSON
  reportName: webhook-comprehensive-test-report
  deleteNamespace: true
  namespaceLabels:
    test: webhook-comprehensive
