---
apiVersion: testworkflows.testkube.io/v1
kind: TestWorkflow
metadata:
  name: agent-basic-test
spec:
  steps:
    - run:
        image: "curlimages/curl:8.13.0"
        # This should likely use Postman, but this is a simple start.
        shell: |
          curl -m 5 -k https://cza-cloudzero-agent-webhook-server-svc.cza.svc.cluster.local/healthz
          curl -m 5 http://cza-cloudzero-state-metrics.cza.svc.cluster.local:8080/metrics
---