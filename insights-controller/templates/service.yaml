apiVersion: v1
kind: Service
metadata:
  name: {{ include "insights-controller.fullname" . }}
  labels:
    {{- include "insights-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "insights-controller.selectorLabels" . | nindent 4 }}
